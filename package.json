{
  "name": "lei.github.io",
  "version": "0.0.1",
  "description": "",
  "author": "LEI",
  "dependencies": {
    "nuxt": "^0.10.7",
    "vue-i18n": "^6.1.1",
    "vue-material": "^0.7.1"
  },
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "pull": "git subtree pull --prefix dist origin master",
    "push": "git subtree push --prefix dist origin master",
    "commit": "cp ./CNAME ./dist/CNAME && git add . && git commit -am 'deploy'",
    "deploy": "npm run generate && npm run commit && npm run push"
  },
  "devDependencies": {
    "vuetify": "^0.11.1"
  }
}
